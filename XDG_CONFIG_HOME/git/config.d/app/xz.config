[tar "txz"]
	command = xz --compress --format=xz -T0 --keep --stdout

[tar "tar.xz"]
	command = xz --compress --format=xz -T0 --keep --stdout

[tar "tlz"]
	command = xz --compress --format=lzma   --keep --stdout

[tar "tar.lzma"]
	command = xz --compress --format=lzma   --keep --stdout
