[tar "tzst"]
	command = zstd --compress -T0 --auto-threads=physical --keep --check --content-size --stdout

[tar "tar.zst"]
	command = zstd --compress -T0 --auto-threads=physical --keep --check --content-size --stdout
